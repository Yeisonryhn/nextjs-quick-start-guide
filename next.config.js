/**
 * @type {import('next').NextConfig}
 */
 const nextConfig = {
    /* config options here */
  }

module.exports = {
    i18n: {
        locales: ['en', 'es'],
        defaultLocale: 'en',
        localeDetection: true,
    },
}
